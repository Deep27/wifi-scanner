import 'package:flutter/widgets.dart';

class LoginFaq extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text("Плохое соединение с интернетом"),
        Text("Забыли 6-значимый пароль для входа")
      ],
    );
  }
}
